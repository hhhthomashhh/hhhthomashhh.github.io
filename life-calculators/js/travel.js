// 15 Travel budget
function travelBudget(c){return layout(c,`<section class="panel"><div class="form-grid">${field('tbTrav','Travellers',2,{step:'1',max:'100'})}${field('tbDays','Travel days',7,{step:'1',max:'365'})}${field('tbHotel','Accommodation total',1400,{prefix:'$'})}${field('tbFood','Food / day / person',60,{prefix:'$'})}${field('tbTrans','Transportation',300,{prefix:'$'})}${field('tbFlights','Flights',1200,{prefix:'$'})}${field('tbActivities','Activities',400,{prefix:'$'})}${field('tbShopping','Shopping',300,{prefix:'$'})}${field('tbMisc','Miscellaneous',200,{prefix:'$'})}<div class="field"><label for="tbRate">Currency multiplier (optional)</label><input id="tbRate" type="number" inputmode="decimal" min="0" step="any" value="1"><div class="hint">Leave at 1 if entries are already in your preferred currency.</div></div></div><div class="button-row"><button class="primary-btn" id="tbCalc">Calculate</button><button class="ghost-btn" id="tbReset">Reset</button></div><div id="tbErr"></div></section><div id="tbOut"></div>`)}
function initTravelBudget(){const calc=()=>{const trav=Math.round(getNum('tbTrav')),days=Math.round(getNum('tbDays')),hotel=getNum('tbHotel'),food=getNum('tbFood'),trans=getNum('tbTrans'),flights=getNum('tbFlights'),activities=getNum('tbActivities'),shopping=getNum('tbShopping'),misc=getNum('tbMisc'),rate=getNum('tbRate');if(!validNums([trav,days,hotel,food,trans,flights,activities,shopping,misc,rate])||trav<1||days<1||rate<=0||[hotel,food,trans,flights,activities,shopping,misc].some(x=>x<0)){document.getElementById('tbErr').innerHTML=error('Enter at least one traveller and day, with non-negative costs and a positive currency multiplier.');return}const items=[['Accommodation',hotel],['Food',food*days*trav],['Transportation',trans],['Flights',flights],['Activities',activities],['Shopping',shopping],['Miscellaneous',misc]].map(([n,v])=>[n,v*rate]);const total=items.reduce((s,x)=>s+x[1],0),per=total/trav,perDay=total/days,ppd=total/(trav*days);let cum=0;const stops=total>0?items.map(x=>{const start=cum/total*360;cum+=x[1];const end=cum/total*360;return `${['#386a5b','#7a8f85','#a68e6b','#9a7e83','#7e8daa','#8f9a68','#aa8870'][items.indexOf(x)]} ${start}deg ${end}deg`}).join(','):'var(--surface-2) 0deg 360deg';document.getElementById('tbErr').innerHTML='';document.getElementById('tbOut').innerHTML=resultBox('Estimated trip total',fmtMoney(total),`${fmtMoney(per)} per person.`,[['Cost per day',fmtMoney(perDay)],['Per person per day',fmtMoney(ppd)],['Travellers',String(trav)]])+`<section class="panel"><h3>Expense breakdown</h3><div class="pie" style="background:conic-gradient(${stops})"></div><div class="legend">${items.map(x=>`<div><span>${x[0]}</span><strong>${fmtMoney(x[1])}</strong></div>`).join('')}</div></section>`;setResultText(`Estimated trip total: ${fmtMoney(total)} for ${trav} traveller${trav===1?'':'s'} over ${days} days, or ${fmtMoney(ppd)} per person per day.`)};document.getElementById('tbCalc').onclick=calc;document.getElementById('tbReset').onclick=()=>location.reload();calc()}

// 16 Fuel cost
function fuelCost(c){return layout(c,`<section class="panel"><div class="form-grid">${field('fuelDist','Trip distance',500,{step:'.1'})}${selectField('fuelDistUnit','Distance unit',[['km','kilometres'],['mi','miles']],'km')}${field('fuelEco','Fuel economy',8.5,{step:'.1'})}${selectField('fuelEcoUnit','Fuel economy unit',[['l100','L/100 km'],['kml','km/L'],['mpgus','MPG US'],['mpgimp','MPG Imperial']],'l100')}${field('fuelPrice','Fuel price',1.75,{prefix:'$',step:'.001'})}${selectField('fuelPriceUnit','Fuel price unit',[['litre','per litre'],['gallon-us','per US gallon'],['gallon-imp','per Imperial gallon']],'litre')}</div><div class="button-row"><button class="primary-btn" id="fuelCalc">Calculate</button><button class="ghost-btn" id="fuelReset">Reset</button></div><div id="fuelErr"></div></section><div id="fuelOut"></div>`)}
function initFuelCost(){const calc=()=>{const d=getNum('fuelDist'),e=getNum('fuelEco'),p=getNum('fuelPrice'),du=document.getElementById('fuelDistUnit').value,eu=document.getElementById('fuelEcoUnit').value,pu=document.getElementById('fuelPriceUnit').value;if(!validNums([d,e,p])||d<=0||e<=0||p<0){document.getElementById('fuelErr').innerHTML=error('Enter a non-negative distance and price, and fuel economy greater than zero.');return}const km=du==='km'?d:d*1.609344;let l100;if(eu==='l100')l100=e;else if(eu==='kml')l100=100/e;else if(eu==='mpgus')l100=235.214583/e;else l100=282.480936/e;const litres=km*l100/100;let priceL=p;if(pu==='gallon-us')priceL=p/3.785411784;else if(pu==='gallon-imp')priceL=p/4.54609;const cost=litres*priceL,costUnit=du==='km'?cost/d:cost/d;document.getElementById('fuelErr').innerHTML='';document.getElementById('fuelOut').innerHTML=resultBox('Estimated fuel cost',fmtMoney(cost,2),`About ${fmtNum(litres,1)} litres required.`,[['Fuel required',`${fmtNum(litres,1)} L`],[`Cost per ${du==='km'?'km':'mile'}`,fmtMoney(costUnit,3)],['Equivalent economy',`${fmtNum(l100,2)} L/100 km`]]);setResultText(`A ${fmtNum(d,1)} ${du} trip at ${fmtNum(l100,2)} L/100 km uses about ${fmtNum(litres,1)} L and costs approximately ${fmtMoney(cost,2)}.`)};document.getElementById('fuelCalc').onclick=calc;document.getElementById('fuelReset').onclick=()=>location.reload();calc()}

// 17 BMI
function bmi(c){return layout(c,`<section class="panel"><div class="segmented"><button class="seg-btn active" data-bmiunit="metric">Metric</button><button class="seg-btn" data-bmiunit="imperial">Imperial</button></div><div class="form-grid" style="margin-top:14px"><div id="bmiMetric">${field('bmiCm','Height',170,{suffix:'cm'})}${field('bmiKg','Weight',70,{suffix:'kg'})}</div><div id="bmiImperial" style="display:none">${field('bmiIn','Height',67,{suffix:'in'})}${field('bmiLb','Weight',154,{suffix:'lb'})}</div></div><div class="button-row"><button class="primary-btn" id="bmiCalc">Calculate</button><button class="ghost-btn" id="bmiReset">Reset</button></div><div id="bmiErr"></div></section><div id="bmiOut"></div>`)}
function initBmi(){let unit='metric';document.querySelectorAll('[data-bmiunit]').forEach(b=>b.onclick=()=>{unit=b.dataset.bmiunit;document.querySelectorAll('[data-bmiunit]').forEach(x=>x.classList.toggle('active',x===b));document.getElementById('bmiMetric').style.display=unit==='metric'?'contents':'none';document.getElementById('bmiImperial').style.display=unit==='imperial'?'contents':'none';calc()});const calc=()=>{let bmiVal;if(unit==='metric'){const cm=getNum('bmiCm'),kg=getNum('bmiKg');if(!validNums([cm,kg])||cm<=0||kg<=0){document.getElementById('bmiErr').innerHTML=error('Height and weight must be greater than zero.');return}bmiVal=kg/Math.pow(cm/100,2)}else{const inch=getNum('bmiIn'),lb=getNum('bmiLb');if(!validNums([inch,lb])||inch<=0||lb<=0){document.getElementById('bmiErr').innerHTML=error('Height and weight must be greater than zero.');return}bmiVal=703*lb/(inch*inch)}const cat=bmiVal<18.5?'Underweight':bmiVal<25?'Healthy range':bmiVal<30?'Overweight range':'Obesity range';document.getElementById('bmiErr').innerHTML='';document.getElementById('bmiOut').innerHTML=resultBox('BMI',fmtNum(bmiVal,1),cat,[['Underweight','< 18.5'],['Healthy range','18.5–24.9'],['Overweight','25.0–29.9'],['Obesity range','30.0+']])+`<section class="panel"><div class="interpret">BMI is a general screening measure and does not account for muscle mass, body composition, age, or individual health circumstances.</div></section>`;setResultText(`BMI estimate: ${fmtNum(bmiVal,1)} (${cat}). BMI is a general screening measure, not a diagnosis.`)};document.getElementById('bmiCalc').onclick=calc;document.getElementById('bmiReset').onclick=()=>location.reload();calc()}
