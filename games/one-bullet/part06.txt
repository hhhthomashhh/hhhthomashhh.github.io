ion drawSwitches(){for(const s of world.switches){ctx.fillStyle=s.active?'#6ed99d':'#f1c63f';roundRect(s.x,s.y,s.w,s.h,5,true,false);ctx.fillStyle='#10151d';ctx.fillRect(s.x+7,s.y+5,6,18)}}
function drawPanels(){for(const p of world.panels){ctx.fillStyle=p.active?'#37596a':'#2b7d9f';roundRect(p.x,p.y,p.w,p.h,4,true,false);ctx.strokeStyle='#72d8ff';ctx.strokeRect(p.x+2,p.y+2,p.w-4,p.h-4);ctx.fillStyle='#8fe8ff';ctx.font='1000 18px system-ui';ctx.fillText('⚡',p.x+4,p.y+24)}}
function drawEnemies(){for(const e of world.enemies){ctx.save();ctx.translate(e.x,e.y);if(!e.alive){ctx.globalAlpha=.3;ctx.rotate(.9);ctx.translate(0,8)}let col='#d95a5a',label='G';if(e.type==='armored'){col='#6f7b88';label='A'}else if(e.type==='heavy'){col='#655f68';label='H'}else if(e.type==='boss'){col='#4c4a52';label='B'}else if(e.type==='nervous'){col='#d58b4e';label='N'}else if(e.type==='sniper'){col='#8d62b0';label='S'}else if(e.type==='shield'){col='#d95a5a';label='G'}ctx.fillStyle=col;ctx.beginPath();ctx.arc(0,0,e.r,0,TAU);ctx.fill();ctx.fillStyle='#1b2028';ctx.fillRect(-11,-6,22,15);ctx.fillStyle='#fff';ctx.font='900 11px system-ui';ctx.textAlign='center';ctx.fillText(label,0,4);ctx.fillStyle='#20262e';ctx.fillRect(-13,18,9,19);ctx.fillRect(4,18,9,19);if(e.type==='armored'||e.type==='heavy'||e.type==='boss'){ctx.strokeStyle='#b7c1ce';ctx.lineWidth=4;ctx.beginPath();ctx.arc(0,0,e.r+4,Math.PI,TAU);ctx.stroke()}if(e.type==='nervous'){ctx.strokeStyle='#f7cf69';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(-10,-24);ctx.lineTo(-3,-30);ctx.moveTo(10,-24);ctx.lineTo(3,-30);ctx.stroke()}ctx.restore()}}
function drawPlayer(){const p=world.player,ang=Math.atan2(aim.y-p.y,aim.x-p.x);ctx.save();ctx.translate(p.x,p.y);ctx.fillStyle='#e7edf4';ctx.beginPath();ctx.arc(0,0,18,0,TAU);ctx.fill();ctx.fillStyle='#202a37';ctx.fillRect(-10,-5,20,13);ctx.rotate(ang);ctx.fillStyle='#e4c56d';ctx.fillRect(10,-4,34,8);ctx.fillStyle='#29313e';ctx.fillRect(35,-6,18,12);ctx.restore()}
function drawAim(){const p=world.player,dx=aim.x-p.x,dy=aim.y-p.y,l=Math.hypot(dx,dy)||1,ux=dx/l,uy=dy/l,max=260;ctx.save();ctx.strokeStyle='rgba(246,201,69,.78)';ctx.lineWidth=2;ctx.setLineDash([10,9]);ctx.beginPath();ctx.moveTo(p.x+ux*32,p.y+uy*32);ctx.lineTo(p.x+ux*max,p.y+uy*max);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle='#f6c945';ctx.lineWidth=2;ctx.beginPath();ctx.arc(aim.x,aim.y,13,0,TAU);ctx.stroke();ctx.beginPath();ctx.moveTo(aim.x-19,aim.y);ctx.lineTo(aim.x-7,aim.y);ctx.moveTo(aim.x+7,aim.y);ctx.lineTo(aim.x+19,aim.y);ctx.moveTo(aim.x,aim.y-19);ctx.lineTo(aim.x,aim.y-7);ctx.moveTo(aim.x,aim.y+7);ctx.lineTo(aim.x,aim.y+19);ctx.stroke();ctx.restore()}
function drawProjectiles(){if(bullet?.alive){ctx.strokeStyle='rgba(246,201,69,.35)';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(bullet.x-bullet.vx*.035,bullet.y-bullet.vy*.035);ctx.lineTo(bullet.x,bullet.y);ctx.stroke();ctx.fillStyle='#fff4b4';ctx.beginPath();ctx.arc(bullet.x,bullet.y,4,0,TAU);ctx.fill()}for(const p of secondary){ctx.fillStyle='#ff7a70';ctx.beginPath();ctx.arc(p.x,p.y,3,0,TAU);ctx.fill()}}
function drawParticles(){for(const p of particles){ctx.globalAlpha=clamp(p.life/.5,0,1);ctx.fillStyle=p.kind==='boom'?(Math.random()>.5?'#ffbf47':'#ff6b45'):p.kind==='glass'?'#8fd7f1':p.kind==='electric'?'#75ddff':p.kind==='fiber'?'#d6bd8e':p.kind==='enemy'?'#c6615f':'#e8e8e8';ctx.fillRect(p.x,p.y,p.size||3,p.size||3)}ctx.globalAlpha=1}
function drawFloating(){ctx.font='900 14px system-ui';ctx.textAlign='center';for(const f of floating){ctx.globalAlpha=clamp(f.life/.7,0,1);ctx.fillStyle='#f6c945';ctx.fillText(f.text,f.x,f.y)}ctx.globalAlpha=1;ctx.textAlign='left'}
function roundRect(x,y,w,h,r,fill,stroke){ctx.beginPath();ctx.roundRect?ctx.roundRect(x,y,w,h,r):(ctx.rect(x,y,w,h));if(fill)ctx.fill();if(stroke)ctx.stroke()}
function burst(x,y,kind){const count=kind==='boom'?30:kind==='glass'?14:kind==='electric'?16:9;for(let i=0;i<count;i++){const a=Math.random()*TAU,s=kind==='boom'?80+Math.random()*230:40+Math.random()*120;particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,gravity:kind==='boom'?180:80,life:.2+Math.random()*.45,size:2+Math.random()*4,kind})}}
function sparkAt(x,y){burst(x,y,'spark')}function spawnMuzzle(x,y,a){for(let i=0;i<8;i++){const aa=a+(Math.random()-.5)*.4,s=70+Math.random()*120;particles.push({x:x+Math.cos(a)*35,y:y+Math.sin(a)*35,vx:Math.cos(aa)*s,vy:Math.sin(aa)*s,life:.12+Math.random()*.14,size:2+Math.random()*3,kind:'boom'})}}
function updateHud(){if(!world)return;const dead=world.enemies.filter(e=>!e.alive).length;$('#hudTargets').textContent=`${dead}/${world.enemies.length}`;$('#hudChain').textContent=`×${stats?.chain||0}`}
function loop(ts){const dt=Math.min(.035,(ts-lastTs)/1000||0);lastTs=ts;if($('#game').classList.contains('active')&&!paused){acc+=dt;const fixed=1/120;let guard=0;while(acc>=fixed&&guard++<8){step(fixed);acc-=fixed}}draw();requestAnimationFrame(loop)}
requestAnimationFrame(loop);

// ---------- UI ----------
function refreshAllUI(){const ts=totalStars();$('#menuStars').textContent=ts;$('#menuPerfect').textContent=Object.values(save.perfect).filter(Boolean).length;$('#menuAch').textContent=Object.values(save.achievements).filter(Boolean).length;renderLevels();renderAchievements();$('#soundToggle').textContent=save.settings.sound?'ON':'OFF';$('#musicToggle').textContent=save.settings.music?'ON':'OFF';$('#shakeToggle').textContent=save.settings.reducedShake?'ON':'OFF';const dk=dateKey();$('#dailyBtn').textContent=save.daily[dk]?'SHOT OF THE DAY ✓':'SHOT OF THE DAY'}
function renderLevels(){const body=$('#levelSelectBody');body.innerHTML='';for(const [chapter,info] of Object.entries(chapterInfo)){const ls=levels.filter(l=>l.chapter===chapter);const sec=document.createElement('div');sec.className='chapter';sec.innerHTML=`<div class="chapterHead"><h3>${chapter.toUpperCase()}</h3><span>${info.desc} · ${info.minStars?info.minStars+' stars to enter':'open'}</span></div>`;const grid=document.createElement('div');grid.className='levelGrid';for(const l of ls){const unlocked=levelUnlocked(l.n);const st=save.stars[l.n]||0;const b=document.createElement('button');b.className='levelBtn'+(unlocked?'':' locked');b.disabled=!unlocked;b.innerHTML=`<div class="levelNum">LEVEL ${l.n}</div><div class="levelName">${l.name}</div><div class="stars">${'★'.repeat(st)}<span class="off">${'★'.repeat(3-st)}</span></div>${save.perfect[l.n]?'<div class="perfectMark">🎯 PERFECT</div>':''}`;if(unlocked)b.onclick=()=>startLevel(l.n);grid.appendChild(b)}sec.appendChild(grid);body.appendChild(sec)}}
function renderAchievements(){const g=$('#achievementGrid');g.innerHTML='';for(const a of achievements){const got=!!save.achievements[a[0]],d=document.createElement('div');d.className='ach'+(got?'':' locked');d.innerHTML=`<div class="achIcon">${got?a[1]:'🔒'}</div><div class="achName">${a[2]}</div><div class="achDesc">${a[3]}</div>`;g.appendChild(d)}}
function showToast(text){const t=$('#toast');t.textContent=text;t.classList.add('show');clearTimeout(showToast._t);showToast._t=setTimeout(()=>t.classList.remove('show'),1300)}
$$('[data-screen]').forEach(b=>b.addEventListener('click',()=>showScreen(b.dataset.screen)));
$('#playBtn').onclick=()=>startLevel(firstPlayable());
$('#dailyBtn').onclick=()=>startLevel(dailyLevel(),{daily:true});
$('#retryBtn').onclick=retry;$('#backGameBtn').onclick=()=>showScreen('levels');$('#hintBtn').onclick=()=>{if(!fired&&!resultState)showHint();else if(resultState==='fail')showHint()};
$('#pauseBtn').onclick=()=>{if(resultState)return;paused=true;$('#pauseOverlay').classList.add('show')};$('#resumeBtn').onclick=()=>{paused=false;$('#pauseOverlay').classList.remove('show');lastTs=performance.now()};$('#pauseRestartBtn').onclick=retry;$('#pauseMenuBtn').onclick=()=>showScreen('menu');$('#pauseSettingsBtn').onclick=()=>showScreen('settings');
$('#soundToggle').onclick=()=>{save.settings.sound=!save.settings.sound;writeSave();refreshAllUI();if(save.settings.sound)sfx('switch')};$('#musicToggle').onclick=()=>{save.settings.music=!save.settings.music;ensureAudio();writeSave();refreshAllUI();updateMusic()};$('#shakeToggle').onclick=()=>{save.settings.reducedShake=!save.settings.reducedShake;writeSave();refreshAllUI()};$('#tutorialBtn').onclick=()=>startLevel(1);$('#resetBtn').onclick=()=>{if(confirm('Reset all ONE BULLET progress on this browser?'))resetSave()};
window.addEventListener('keydown',e=>{if(e.key==='r'||e.key==='R'){if($('#game').classList.contains('active'))retry()}else if(e.key==='Escape'){if($('#game').classList.contains('active')){$('#pauseOverlay').classList.add('show');paused=true}}else if(e.code==='Space'){if($('#game').classList.contains('active')){e.preventDefault();fire()}}});
window.addEventListener('blur',()=>{if($('#game').classList.contains('active')&&!resultState){paused=true;$('#pauseOverlay').classList.add('show')}});
document.addEventListener('touchmove',e=>{if($('#game').classList.contains('active'))e.preventDefault()},{passive:false});

refreshAllUI();resizeCanvas();
})();
</script>
</body>
</html>
